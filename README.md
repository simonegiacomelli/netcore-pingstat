# PingStat

Small .NET console app that pings a list of hosts and logs their up/down status.

## Build

```bash
dotnet build PingStat.csproj
```

## Run

```bash
dotnet run --project PingStat.csproj
```

Hosts and poll interval are configured in `PingStat.ini`.

## Test

```bash
dotnet test
```
